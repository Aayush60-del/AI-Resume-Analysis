# Vercel → Settings → Environment Variables → then REDEPLOY (build-time only)
# Must include /api — signup hits: {VITE_API_URL}/auth/signup
VITE_API_URL=https://YOUR-SERVICE-NAME.onrender.com/api

# Wrong (causes "Route not found"):
# VITE_API_URL=https://YOUR-SERVICE-NAME.onrender.com
